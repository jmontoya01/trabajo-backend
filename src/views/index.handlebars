<h1>Mis productos</h1>
<div class="containerCards">
    {{#each products}}
    <div class="card">
        <p>{{this.title}} </p>
        <p>{{this.description}} </p>
        <p>{{this.price}} </p>
    </div>
    {{/each}}
</div>